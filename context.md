I have a virtual env that I am in right now, its named ragent-env
in conda. Before we start, i am using a macbook pro intel i9 on macos 13. 
  sentence-transformers==2.7.0
  transformers==4.40.2
  torch==2.2.2
  numpy==1.26.4
  pandas==2.3.3

I have to stick to this set of library versions because any version higher than this wont work.
Here is what I want you to do. Write very clean code, no slop, easy to understand, highly efficient. any progress you make - store it in a progress.md file (what counts as a
progress? a feature or a bug which we encountered/library dependency error/ version error/ its solution/ the features which have been implemented from phases in the roadmap)
only a small gist would be fine, it does not have to be detailed.

If we do find bugs or code which is not running as intended, I want a root cause analysis and explaination of the error and the most likely - highly suggested fix before you
edit any code.